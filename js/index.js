/**
 * 
 * @param {*} ruta 
 * función que tendrá el parámetro de una ruta y abrirá una nueva ventana con el nombre de esa ruta
 */
 const crear=(direccion)=>{

    window.open(direccion, "resizable", "width=500, height=300");

}